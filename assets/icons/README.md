# assets/icons/

Reserved for additional icon sizes/formats (for example a PNG favicon
fallback or Apple touch icon) if added later. Currently empty — the site
uses `favicon.svg` at the repository root for all icon needs.
